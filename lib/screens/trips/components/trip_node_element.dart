import 'package:flutter/material.dart';

class TripNodeElement extends StatelessWidget {
  const TripNodeElement({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
